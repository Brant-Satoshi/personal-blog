# 路线图

阶段性目标与里程碑。已完成部分按时间排列；候选池未排序——动手前先在这里排期，并与 `docs/PRD.md` 对齐。

## 已完成

- **2026-02**：项目脚手架、最小博客（首页 + `/[slug]` 详情）、lint/typecheck CI
- **2026-05**：shadcn 重设计、深色模式、文章 TOC、分类页、中英 i18n、站内搜索、Docker/GHCR 部署流水线上线
- **2026-07**：markdown 管线抽取（shiki 高亮、mermaid、code group、复制按钮）、文章点赞（全局计数）、SEO/RSS 基础设施（robots、sitemap、feed.xml、社交图、JSON-LD）、翻译文章、代码去重与全部文档刷新
- **2026-07**：全站静态化（锁定 zh、移除语言切换）、zod frontmatter 校验与草稿支持、标签/系列/归档/分页路由、相关文章与前后篇、正文图片灯箱、构建期搜索索引与打分搜索、`SITE_URL` 构建校验（verify:seo）、vitest 测试接入 CI、系统字体栈、移除 shadcn 残留

## 候选池（未排序）

- 文章评论系统
- 阅读量统计（接入隐私友好的访问统计，用真实数据驱动热门榜单）
- 为有真实译文的文章增加 `/zh`、`/en` URL 与 `hreflang`
- 为图片生成多尺寸 AVIF/WebP 衍生文件
- 增加浏览器级无障碍和视觉回归测试

## 扩展条件

- 单实例点赞量显著增长时，将 JSON 存储迁移到 SQLite
- 部署多实例时，将点赞和限流迁移到共享存储
- 文章数量达到约 100 篇后，评估分页大小和搜索索引分片
